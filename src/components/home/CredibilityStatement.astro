<section class="credibility" aria-labelledby="credibility-title">
  <header>
    <p class="credibility__kicker">Signals, not slogans</p>
    <h2 id="credibility-title">Shipped across the arc.</h2>
  </header>
  <div class="credibility__proofs">
    <a href="#career-atlas">
      <strong>15+ years</strong>
      <span>Games → simulation → design tech → production AI</span>
    </a>
    <a href="/work/humanoid-robot-control-system/">
      <strong>1 patent</strong>
      <span>Humanoid robot control system · public record</span>
    </a>
    <a href="https://www.linkedin.com/feed/update/urn:li:activity:7315954209030320129/">
      <strong>Speed + execution</strong>
      <span>Team award for turning difficult systems into shipped work ↗</span>
    </a>
  </div>
</section>

<style>
  .credibility {
    display: grid;
    grid-template-columns: minmax(18rem, 0.72fr) 2fr;
    gap: clamp(1.5rem, 4vw, 4.5rem);
    align-items: end;
    padding: clamp(5rem, 10vw, 9rem) var(--page-gutter);
    border-bottom: 1px solid var(--line);
  }

  .credibility__kicker {
    color: var(--signal);
    font-size: 0.76rem;
    font-weight: 720;
  }

  h2 {
    max-width: 10ch;
    margin: 0;
    font-size: clamp(2.65rem, 5.7vw, 5.7rem);
    font-weight: 740;
    line-height: 0.92;
  }

  .credibility__proofs {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    border-block: 1px solid var(--line);
  }

  .credibility__proofs a {
    display: grid;
    min-height: 11rem;
    padding: 1.25rem;
    align-content: space-between;
    border-right: 1px solid var(--line);
    text-decoration: none;
    transition: background-color 220ms var(--ease-out), color 220ms var(--ease-out), transform 220ms var(--ease-out);
  }

  .credibility__proofs a:last-child { border-right: 0; }
  .credibility__proofs strong { font-size: clamp(1.35rem, 2.4vw, 2.2rem); line-height: 1; }
  .credibility__proofs span { max-width: 24ch; color: var(--text-muted); font-size: 0.72rem; }
  .credibility__proofs a:hover {
    background: var(--text);
    color: var(--surface);
    transform: translateY(-0.25rem);
  }
  .credibility__proofs a:hover span { color: inherit; }

  @media (max-width: 68rem) {
    .credibility { grid-template-columns: 1fr; align-items: start; }
    h2 { max-width: 12ch; }
  }

  @media (max-width: 46rem) {
    .credibility__proofs { grid-template-columns: 1fr; }
    .credibility__proofs a { min-height: 7.5rem; border-right: 0; border-bottom: 1px solid var(--line); }
    .credibility__proofs a:last-child { border-bottom: 0; }
  }
</style>
